# 🧠 Searching and Sorting Algorithms Quiz

✅ **Passing Requirement:** You must correctly answer **at least 9 out of 10 questions**.

---

## 1️⃣ Which search algorithm iterates through a list of items, checking each item from the beginning until the target item is found?

- Bubble search  
- ✅ **Linear search**  
- Merge search  
- Binary search  

---

## 2️⃣ Which technique is used to break down a problem into smaller sub-problems?

- Divide-and-solve  
- Break-down-and-solve  
- ✅ **Divide-and-conquer**  
- Break-down-and-conquer  

---

## 3️⃣ What is the space complexity of the linear search algorithm?

- O(log n)  
- ✅ **O(1)**  
- O(n)  
- O(n²)  

---

## 4️⃣ Which is more suitable for a larger list of items between binary search and linear search?

- Both  
- ✅ **Binary search**  
- None  
- Linear search  

---

## 5️⃣ What is the space complexity of binary search?

- O(n)  
- ✅ **O(1)**  
- O(n log n)  
- O(log n)  

---

## 6️⃣ What does the binary search algorithm return when it finds the target item?

- The size of the list searched.  
- ✅ **The index of the target item.**  
- A boolean value of the target.  
- The value of the target item.  

---

## 7️⃣ What does the linear search algorithm return if it doesn't find the target item?

- Null  
- ✅ **-1**  
- Undefined  
- 0  

---

## 8️⃣ How does the merge sort algorithm work?

- It builds a sorted list by repeatedly finding the smallest element.  
- ✅ **It recursively splits the list into smaller items, then merges them in sorted order.**  
- It searches for an element by checking each item one by one.  
- It swaps adjacent elements until the list is sorted.  

---

## 9️⃣ What is the condition for a binary search to work?

- The list must have an even number of elements.  
- The list must contain only unique values.  
- The list must contain no negative numbers.  
- ✅ **The list must be sorted.**  

---

## 🔟 Which computer science approach is used to implement the merge sort algorithm?

- ✅ **Divide-and-conquer**  
- Tracing  
- Greedy algorithm  
- Dynamic programming  

---
